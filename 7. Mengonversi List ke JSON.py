import json

data_list = [1, 2, 3, 4, 5]
json_data = json.dumps(data_list)

print("JSON encoded list:", json_data)
# Fungsi: Mengonversi list Python menjadi format JSON.
# Kondisi: Ketika Anda bekerja dengan struktur data list yang perlu dikonversi.
