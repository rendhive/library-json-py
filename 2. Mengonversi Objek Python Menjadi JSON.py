import json

data = {'key': 'value', 'number': 42}
json_data = json.dumps(data)
print("JSON encoded:", json_data)
# Fungsi: Mengonversi objek Python (dictionary) menjadi format JSON.
# Kondisi: Ketika Anda ingin menyimpan atau mentransmisikan data dalam format JSON.
